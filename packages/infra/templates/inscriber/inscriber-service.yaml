apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.labels.inscriber.name }}
  labels:
    app: {{ .Values.labels.inscriber.name }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.ports.inscriber }}
  selector:
    app: {{ .Values.labels.inscriber.name }}

